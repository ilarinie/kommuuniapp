<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="#ab0e09" />
    <title><%= content_for?(:title) ? yield(:title) : "Kommuuniapp" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : " Kommuuniapp" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
      <%= render 'layouts/messages' %>
      <%= yield %>
    </main>
    <footer class="footer">
      <div class="container">
        <p class="text-muted">
        <% if current_user %>  Logged in as <%= current_user %> | XP: <%= current_user.xp_total %> <% end %>
        </p>
      </div>
    </footer>
  </body>
</html>
