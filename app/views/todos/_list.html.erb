

<table class="table table-hover">
  <thead>
    <th>
      Title
    </th>
    <th>
      Creator
    </th>
    <th>
      Due in
    </th>
    <th>
      Solve!
    </th>
  </thead>
  <tbody>
    <% todos.each do |todo| %>
    <tr>
      <td>
        <%= link_to todo.title, todo_path(todo) %>
      </td>
      <td>
        <%= User.find(todo.creator_id).to_s %>
      </td>
      <td>
        <%= todo.due %>
      </td>
      <td>
        <%= link_to 'Solve!', todo_path(todo), class:"btn btn-primary" %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= will_paginate todos, renderer: BootstrapPagination::Rails %>
