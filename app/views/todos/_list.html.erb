<%= will_paginate todos, renderer: BootstrapPagination::Rails %>

<table class="table table-hover">
  <thead>
    <th>
      Title
    </th>
    <th>
      Creator
    </th>
    <th>
      Due in
    </th>
  </thead>
  <tbody>
    <% todos.each do |todo| %>
    <tr>
      <td>
        <%= link_to todo.title, todo_path(todo) %>
      </td>
      <td>
        <%= User.find(todo.creator_id).to_s %>
      </td>
      <td>
        <%= todo.due %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
